# ==========================================
# Telegram Gateway - 环境变量配置模板
# ==========================================
# 使用说明：
# 1. 复制此文件并重命名为 .env
# 2. 填写所有必需的配置项
# 3. 不要将 .env 文件提交到 Git

# ==========================================
# 后端服务配置 (必需)
# ==========================================
BACKEND_URL=http://localhost:8000
BACKEND_WEBHOOK=http://localhost:8000/api/messages/inbox
BACKEND_INTERNAL_SESSIONS=http://localhost:8000/settings/internal/telegram/sessions

# ==========================================
# Gateway 安全配置 (必需)
# ==========================================
# 用于验证请求，必须与 backend/.env 中的 TELEGRAM_GATEWAY_SECRET 一致
GATEWAY_SECRET=your-gateway-secret-change-me

# ==========================================
# Telegram API 配置 (可选 - 单客户端回退模式)
# ==========================================
# 在 https://my.telegram.org/apps 获取
# 注意：如果使用多用户模式，可以留空
API_ID=0
API_HASH=
TG_SESSION=tg_gateway
